{
  "name": "mandir-receipt-app",
  "version": "1.0.0",
  "description": "Mandir Receipt Manager - Mobile App",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step needed - pure HTML/CSS/JS'",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open:android": "npx cap open android",
    "cap:run:android": "npx cap run android",
    "setup": "npm install && npx cap add android && npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "keywords": ["mandir", "temple", "receipt", "donation"],
  "author": "",
  "license": "ISC"
}
